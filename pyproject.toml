[project]
name = "authoserviceproject"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "autoservice-core",
    "fastapi>=0.136.3",
    "httpx>=0.28.1",
    "pytest>=9.0.3",

    "sqlalchemy>=2.0.50",

]

[tool.uv.workspace]
members = [
    "lib/autoservice_core",
]

[tool.uv.sources]
autoservice-core = { workspace = true }

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
